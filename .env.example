# Local development values only. Do not reuse these credentials outside local Docker Compose.
POSTGRES_DB=ticketrush
POSTGRES_USER=ticketrush_app
POSTGRES_PASSWORD=ticketrush_local_only
POSTGRES_PORT=5432
REDIS_PORT=6379
