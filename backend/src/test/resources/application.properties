# Integration tests invoke HoldExpiryService explicitly when they need to verify expiry.
# Prevent the background sweep from running after a Testcontainers PostgreSQL instance shuts down.
app.holds.expiry.initial-delay=PT24H
app.kafka.enabled=false
